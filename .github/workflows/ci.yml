name: CI

on:
  push:
    branches: ["master", "main", "feature/**"]
  pull_request:
    branches: ["master", "main"]

jobs:
  python-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install tooling
        run: python -m pip install --upgrade pip ruff fastapi pydantic pydantic-settings sqlalchemy alembic "uvicorn[standard]"
      - name: Ruff
        run: ruff check .
      - name: Compile Python
        run: python -m compileall services packages
