# services/policy-svc

权限、安全、审批和预算策略服务。

职责：

- 管理工具调用权限、外部请求白名单、文件访问范围和数据库查询范围。
- 管理模型预算、高成本调用审批和任务级成本上限。
- 管理记忆写入规则和高风险操作阻断。

Beta1.0 默认策略：

- 未知外部域名默认审批或拒绝。
- 禁止工具访问内网保留地址、metadata endpoint 和未授权协议。
- 文件工具只能访问授权工作目录。
- 日志和 trace 默认脱敏。
